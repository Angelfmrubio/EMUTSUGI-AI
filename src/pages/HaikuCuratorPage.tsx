import { HaikuCurator } from '@/components/HaikuCurator';

const HaikuCuratorPage = () => {
  return (
    <div className="min-h-screen bg-gradient-to-b from-neutral-50 to-blue-50">
      <HaikuCurator />
    </div>
  );
};

export default HaikuCuratorPage;
